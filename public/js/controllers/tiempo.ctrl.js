import { initLayout } from './layout.ctrl.js';

document.addEventListener('DOMContentLoaded', () => {
    const user = initLayout('tiempo');
    if (!user) return;

    // Lógica exclusiva de tiempo laborado...
});
